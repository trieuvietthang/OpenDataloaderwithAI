__pycache__/
*.pyc
*.pyo
build/
dist/
*.spec
# LexGuard.spec has real, hard-won build fixes (bundling opendataloader_pdf's
# jar, excluding the optional Docling/torch stack) — worth tracking.
!LexGuard.spec
*.log
.env
.venv
venv/
env/

# Runtime user config: holds live API keys, created by the app on first run.
config.json

# Runtime processing history, created by the app on first run.
lich_su.json

# OCR model caches downloaded at runtime
docling_models/
tessdata/

# Local scratch files from manual testing
Mau_PDF.*
test_*.png
